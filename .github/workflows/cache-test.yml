name: Test CCACHE 50MB+ 最终稳定版
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-22.04
    env:
      CCACHE_DIR: /home/runner/.ccache

    steps:
      - name: 检出代码
        uses: actions/checkout@v6

      - name: 安装依赖
        run: |
          sudo apt update
          sudo apt install -y gcc g++ ccache

      - name: 创建缓存目录
        run: |
          mkdir -p $CCACHE_DIR
          touch $CCACHE_DIR/.keep

      - name: GitHub 缓存保存
        uses: actions/cache@v5
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ${{ runner.os }}-BIGCACHE-50MB-v3

      - name: 启用 ccache（只保留必要变量）
        run: |
          echo "PATH=/usr/lib/ccache:$PATH" >> $GITHUB_ENV
          echo "CCACHE_DIR=$CCACHE_DIR" >> $GITHUB_ENV
          echo "CCACHE_MAXSIZE=10G" >> $GITHUB_ENV
          ccache -z

      - name: 【重型编译】生成 1200 个文件
        run: |
          mkdir -p build
          cd build
          for i in {1..1200}; do
            echo "void func$i() { long a=$i; long b=a*2; long c=b+3; }" > test$i.c
            gcc -c test$i.c -o test$i.o
          done

      - name: 查看缓存结果
        run: |
          echo "===== CCACHE 状态 ====="
          ccache -s
          echo "===== 缓存目录大小 ====="
          du -sh $CCACHE_DIR
